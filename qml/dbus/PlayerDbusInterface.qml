/**
  * Dbus play/stop/next and notification interface for Y-RadioPlayer
  * Note to Harbour QA: This is not used yet!!!
 **/

/*
import QtQuick 2.0
import org.nemomobile.dbus 1.0

QtObject {
    id: dbus

    function currentSong(song) {
        radioPlayerNotification.call("nowPlaying", [song.title, '', song.artist])
    }

    DBusInterface {
        id: radioPlayerNotification

        destination: "org.tal.yradio.notification"
        iface: "org.tal.yradio.notification.Interface"
        path: "/org/tal/yradio/notification"
    }

}
*/
